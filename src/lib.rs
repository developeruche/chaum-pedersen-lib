mod secp256k1; // this is the lib that would be used for elliptic curve operations
mod utils; // this would hold the utils functions used through out the life-cycle of this lib










#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {

        // assert_eq!(result, 4);
    }
}
